rank-bm25
numpy<2.0.0
scikit-learn
torch
transformers>=4.57
accelerate>=0.26.0
peft
langchain-openai
langchain-core
python-dotenv
nltk
spacy
matplotlib
pandas
datasets
optuna
bert-score

# FActScore — install separately from the local directory:
# pip install -e FActScore/ --no-deps

# Also run:
# python -m spacy download en_core_web_sm
